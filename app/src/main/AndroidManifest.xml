<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cms">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewDetails"></activity>
        <activity android:name=".OrgViewReg" />
        <activity android:name=".CandidateSignupActivity" />
        <activity android:name=".StuRegisterConferenceActivity" />
        <activity android:name=".StuViewReviewActivity" />
        <activity android:name=".StuSubmitPaperActivity" />
        <activity android:name=".StuSearchActivity" />
        <activity android:name=".StuActivity" />
        <activity android:name=".ReviewerActivity" />
        <activity android:name=".OrgViewReviewActivity" />
        <activity android:name=".OrgAssignReviewerActivity" />
        <activity android:name=".OrgAddConferenceActivity" />
        <activity android:name=".OrgActivity" />
        <activity android:name=".LoginReviewerActivity" />
        <activity android:name=".LoginOrganiserActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>